# enclava-init

In-TEE Rust replacement for the legacy `bootstrap_script.sh`. Runs as a
Kubernetes initContainer inside a Kata SEV-SNP guest. Performs Argon2id-based
password unlock or KBS-fetched autounlock, opens both LUKS devices (state and
tls-state), runs the in-TEE Trustee policy verification chain, writes
per-component HKDF-derived seeds, and exits 0.

## Build dependencies

`libcryptsetup-rs` 0.15 binds to libcryptsetup ≥ 2.2.0 via pkg-config + bindgen
and links against `libcryptsetup.so.12` at runtime. Container builds need:

| Distro                                        | Package                                             |
| --------------------------------------------- | --------------------------------------------------- |
| Debian/Ubuntu (Dockerfile FROM debian:slim)   | `libcryptsetup-dev`                                 |
| Fedora/RHEL                                   | `cryptsetup-devel libuuid-devel device-mapper-devel json-c-devel` |

`bindgen` also needs the clang resource headers (`/usr/lib/clang/<ver>/include`)
on the include path — install `clang` (or `libclang-dev` on Debian).

If those packages are missing the workspace fails at link time. Do not stub
the LUKS module — Phase 5 requires real cryptsetup.

## Configuration

Loaded from `/etc/enclava-init/config.toml` (override via
`ENCLAVA_INIT_CONFIG`). The ConfigMap that backs this file is generated by
`enclava-engine::manifest`.

Two LUKS volumes are always opened: the durable app/state device (`/dev/csi0`
mapped to `cap-state`, mounted at `/state/app-data`) and the disposable
tls-state device (`/dev/csi1` mapped to `cap-tls-state`, mounted at
`/state/tls-state`). Caddy reads its TLS material from the latter.

## Modes

- `autounlock`: fetches the wrap key from KBS via the local CDH endpoint.
- `password`: receives the password over a unix socket from
  attestation-proxy's TLS-pinned `/unlock` POST, derives `owner_seed` via
  Argon2id, retries on bad password, rate limits at 5/60s.

## In-TEE verification

When `trustee-policy-read-available = true` the binary fetches the active
policy from `GET /resource-policy/<id>/body` and the artifact bundle from
`GET /api/v1/workload/artifacts`, then runs the six-step chain in
`trustee_verify::verify_chain` (see `SECURITY_MITIGATION_PLAN.md` rev14
~lines 811-833). Any mismatch refuses to release seeds.

Until Phase 3's Trustee patches deploy, the flag defaults to false and the
binary releases seeds with a loud `tracing::error!` saying verification was
skipped — we do not fall back to a local descriptor file the way an earlier
prototype did.

## Debug-only override

`ENCLAVA_INIT_DEV_NO_LUKS=true` skips LUKS open; only honored when the binary
is built with `cfg(debug_assertions)`. Production release builds ignore it.

## Tests

`cargo test -p enclava-init --lib` runs all unit tests including the
end-to-end Trustee verification chain against a synthetic descriptor +
policy envelope.

LUKS round-trip tests (`luks::tests::format_then_open_round_trip`) are gated
behind the `luks-integration` feature and require a real loopback device.
Run with `cargo test -p enclava-init --features luks-integration -- --ignored`
in CI environments that have privileged loopback access.
