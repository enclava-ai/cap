[advisories]
yanked = "deny"
unmaintained = "none"
ignore = [
    { id = "RUSTSEC-2023-0071", reason = "Transitive via jsonwebtoken/sigstore; CAP does not perform RSA private-key operations, and upstream has no fixed rsa release yet. Revisit when jsonwebtoken/sigstore move off rsa 0.9.x." },
]

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
